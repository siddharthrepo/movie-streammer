# Compiled binaries / build output
/bin/
*.exe
*.test
*.out
# Service binaries emitted by `go build ./cmd/<svc>` into the repo root
/upload-service
/transcode-worker
/stream-service
/outbox-relay

# Local env (never commit secrets; see .env.example)
.env

# Media / scratch (don't commit raw uploads or HLS output)
*.mp4
*.mkv
*.mov
*.ts
*.m4s
*.m3u8
/tmp/

# OS / editor
.DS_Store
.idea/
.vscode/
